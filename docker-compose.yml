version: "3.9"

services:
  toolscout:
    build: .
    container_name: toolscout
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app   # hot-reload: code changes reflect without rebuild
    restart: unless-stopped